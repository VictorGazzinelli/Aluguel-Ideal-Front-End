import * as yup from "yup";

export const schema = yup.object().shape({
    // Cidade: yup.string().required('Este campo é obrigatorio'),
		// Bairro: yup.string().required('Este campo é obrigatorio'),
		// Aluguel: yup.string().required('Este campo é obrigatorio'),
		// Quartos: yup.string().required('Este campo é obrigatorio'),
});